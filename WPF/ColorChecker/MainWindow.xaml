<Window x:Class="ColorChecker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorChecker"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label x:Name="colorArea" Margin="10" Background="Black"/>

        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="R" Text="R"
                       Grid.Column="0" Grid.Row="0" FontSize="30"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock x:Name="G" Text="G"
                       Grid.Column="0" Grid.Row="1"  FontSize="30"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock x:Name="B" Text="B" 
                       Grid.Column="0" Grid.Row="2"  FontSize="30"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <Slider x:Name="rSlider" 
                    Minimum="0" Maximum="255" 
                    Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" 
                    ValueChanged="Slider_ValueChanged"/>
            <Slider x:Name="gSlider" 
                    Minimum="0" Maximum="255"
                    Grid.Column="1" Grid.Row="1" VerticalAlignment="Center"
                    ValueChanged="Slider_ValueChanged"/>
            <Slider x:Name="bSlider"
                    Minimum="0" Maximum="255"
                    Grid.Column="1" Grid.Row="2" VerticalAlignment="Center"
                    ValueChanged="Slider_ValueChanged"/>

            <TextBox x:Name="rValue" 
                     Text="{Binding ElementName=rSlider,Path=Value,
                            UpdateSourceTrigger=PropertyChanged,StringFormat={}{0:0}}"
                     Grid.Row="0" Grid.Column="2" FontSize="15" TextAlignment="Right"
                     VerticalAlignment="Center"/>

            <TextBox x:Name="gValue"
                     Text="{Binding ElementName=gSlider,Path=Value,
                     UpdateSourceTrigger=PropertyChanged,StringFormat={}{0:0}}"
                     Grid.Row="1" Grid.Column="2" FontSize="15" TextAlignment="Right"
                     VerticalAlignment="Center"/>

            <TextBox x:Name="bValue"
                     Text="{Binding ElementName=bSlider,Path=Value,
                     UpdateSourceTrigger=PropertyChanged,StringFormat={}{0:0}}"
                     Grid.Row="2" Grid.Column="2" FontSize="15" TextAlignment="Right"
                     VerticalAlignment="Center"/>

            <ComboBox Grid.Row="3" VerticalAlignment="Center" Grid.ColumnSpan="3" Margin="23,0,23,0"/>

        </Grid>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>
            <Button Content="STOCK"
                    Grid.Column="0" Grid.Row="0" FontSize="15" Margin="10" 
                    VerticalAlignment="Center"/>
            <ListBox Grid.Row="1" Margin="10"/>
        </Grid>
    </Grid>
</Window>
